<div id="dates">
  <% if !@committees.nil? %>
    <% @committees.each do |committee| %> 
      <% @committee = committee %>
      <hr style="background-color: #777; height: 3px; border:none;">
      <table class = "table">
        <h2>
         <button type="button" class="btn btn-default"><%= link_to @committee.date, @committee %></button>
         <button type="button" class="btn btn-default"> <%= link_to 'Промяна на комисия', edit_committee_path(@committee), data: {no_turbolink: true} %></button>
        </h2>                                  
        <thead>        
          <tr>
            <th>Свободен</th>
            <th>Име</th>
            <th>Опции</th>
          </tr> 
        </thead>   
        <%= render 'tbody' %>            
      </table>
      <div>
        <%= render 'add_another_commissioner' %> 
      </div>  
      <br />
    <% end %>   
  <% end %>
</div>  